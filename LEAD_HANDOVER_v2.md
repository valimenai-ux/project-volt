# LEAD_HANDOVER_v2 — FREEZE-ERA ADDENDUM (2026-08-31)

Read after LEAD_HANDOVER.md (doctrine D1-D12, authority, how the
principal works — all still valid) and BASELINE_v7_FREEZE.md (the
governing state). This addendum is what changed between them.

## What happened since LEAD_HANDOVER.md
- WS8 ratified: S1-S4 killed, WHR dropped (final); numbers frozen at
  r3 with open findings (r4 ordered, not run).
- WS9 ran: S6/S4'/S5-13L/S7 provisional ADVANCE on grade-heavy
  regional duty; S5-11L killed; Opus pre-adjudication NOT CLEAN
  (PRE-B1..B3); R38 trip-time gate clarified to every corner —
  S5-13L expected to convert to KILL-ON-TIME; never executed (frozen).
- WS11 ran (R32): V1 Postal +20.11% provisional ADVANCE vs stock NPR
  on VOLT-SUB; V2 Trucker KILLED (-7.93%, a draw at the ruler's
  pessimistic end, never reaching the bar). Doctrine D20 (stop-go
  architecture) and D21 (lower-bound ruler is the wrong guarantee
  for a kill).
- KX not converged after 3 rounds (radiator sizing 103.5 vs 95 kW).
- 2026-08-31: the principal FROZE the research track. No new rounds,
  no adjudications, Fable pass cancelled. Focus: WS12 exhibit
  (app/ASSIGNMENT.md), WS13 publication (README/FINDINGS/METHOD/
  LIMITATIONS/REPRODUCE from the record).
- Repo: github.com/valimenai-ux/project-volt, PRIVATE. Cloud sessions
  push branches; the foreman (or a Sonnet session) merges. The
  Filesystem MCP works only when the principal is on the Mac.
- Model budget: Fable was heavily consumed; Opus for WS12/WS13
  workers; Sonnet for mechanical work. The lead chat itself is the
  only Fable seat left in use.

## The lead's remaining job
1. Supervise WS12 and WS13 to completion: review against the record
   (every number cited, every status as v7 labels it, nothing
   promoted). Cut elements the record cannot feed; never let a
   provisional verdict render as final.
2. Guard the freeze: do not reopen research unless the principal
   directs it. One standing option on the table, not recommended by
   the former lead: WS11 r3 (cheap; the only round that would convert
   the +20.11% headline from provisional to ratified before the tag).
3. Publication decisions: the v1.0-findings tag, visibility flip
   (public = permanent disclosure; note the patent consequence once),
   and the README's honesty (the story is "we killed our own ideas
   on pre-committed criteria and mostly explained the status quo").
4. Answer the principal's questions from disk facts. If a rationale
   is missing from disk, search the prior lead chat in this project
   (conversation_search) before guessing.

## Rules that bit the former lead — keep them
Verify an artifact exists before saying it does (D4). Verify product
claims before relaying them (D11). Pre-registration is dead with the
freeze; the habit of stating expectations before reading a report
is not. Say plainly which half of a principal's instinct is physics
and which is economics (D12).
