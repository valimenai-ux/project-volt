# FINDINGS — WS3 BATTERY PACK — review round 2

Adjudicator, fresh context, 2026-08-29. Basis: BASELINE_v1.md (read in full), WS3_battery/ASSIGNMENT.md, FINDINGS_WS3_r1.md, the reworked REPORT_WS3.md, results.json, tables_ws3.md, regen_acceptance.csv, run_ws3.py, ws3_cells.py, ws3_pack.py, make_report.py, and the ratified WS1 record (results.json + volt_params/volt_physics/volt_cycles/volt_variants).

## Verification performed

- **Determinism: PASS.** Re-ran the delivered pipeline (`.venv/bin/python run_ws3.py` then `.venv/bin/python make_report.py`). results.json, tables_ws3.md, regen_acceptance.csv, REPORT_WS3.md and all four figures regenerate **byte-identical** (sha256 compared against pre-run snapshots).
- **Three-way interface check: PASS.** The machine-readable block in REPORT_WS3.md == the block in tables_ws3.md == `json.dumps(results.json → interface_WS3)`, verbatim (string equality).
- **Independent re-derivations (own implementations, not the worker's code paths).** All of the following reproduce the delivered values exactly (to the printed precision):
  - **Window arithmetic**: 662.4 nominal / 432.0 operating min (288 × 1.50) / 748.8 continuous ceiling / 777.6 transient+protect / OCV window 610.56–696.96 / 27.6 V module; nameplate 15.2352 kWh, usable-at-bus 11.0836, mass 280.52 kg, volume 185.472 L.
  - **R8 gates against the 1.50 V/cell floor** (own capability formula, own 0.5 °C sweep): dis 120 kW over SOC 40–90 from **−5.5 °C**; chg 110 kW over SOC 15–85 from **0.0 °C**; 50 kW continuous from **−10.0 °C** (available at the envelope edge, checked at SOC 0.15 and 0.85 by hand).
  - **T12 derate table, all six rows** (own quadratic solves): floors 1.50/1.60/1.70/1.80/1.85/1.90 V/cell → gates −5.5/−3.0/+0.5/+6.0/**+9.0**/+14.0 °C with I = 274.29/258.98/243.85/230.52/224.50/218.91 A and V_bus = 437.50/463.36/492.10/520.57/534.51/548.17 V — every figure matches `dis_gate_vs_electronics_floor_V` to 0.01.
  - **All five r8_operating_points**: warm SOC 40 = 209.877 A / 571.763 V; cold gate SOC 40 = 274.287 A / 437.498 V; warm SOC 15 = 227.395 A / 527.715 V; charge warm SOC 85 = 149.343 A / 736.558 V; charge at its 0.0 °C gate = 141.676 A / **776.421 V** (above 748.8, inside 777.6 — the declared `transient_max_V` member is verified consistent).
  - **252s bound** (own recompute at 252 series): 13.3308 kWh, 579.6 V nominal, window 378.0–655.2 V (transient 680.4); gates −1.5 °C / +4.5 °C; warm 120 kW = 244.65 A / 490.49 V; at its own gate 314.23 A / 381.88 V — matches `series_252_bound` exactly.
  - **Descent sweep** (own road-load formula from the BASELINE vehicle parameters + own electro-thermal integrator): the 8 GVW rows reproduce WS1's §4.6 retardation table exactly (22.046…45.565 kW; worst row 70 km/h → 39.4423 kW at bus); T4's 25 km/h row 5.39 / 2.24 / 0.00 kWh, peak 45.7 °C, ceiling 1017 s; **+20% payload mass 7,180 kg** (= 3,700 + 1.2 × 2,900, WS1 run_ws1.py 4a convention, confirmed at source), worst resistor **44.3177 kW** at 70 km/h, margin **5.6823 kW**, friction 0.0000 kWh on every row at both +45 °C and −10 °C, worst-row split 5.47 / 0.87 / 0.00 kWh, peak 47.0 °C — matches `descent_resistor_bound` and the T4 note exactly.
  - **Literal 50 kW × 24 min at +45 °C** (own integrator): peak 50.33 °C, end heat 1.4096 kW, 24-min average 1.4193 kW, steady 51.8 °C — matches T4, the ledger's AVERAGE and SIZING lines, and `coolant.heat_rejection_cont_kW`.
  - **Cold-case correction (F3)**: rebuilt the cold V1 reference case through WS1's own `four_numbers` (aux 4 kW, cap 75 kW), then computed the −10 °C model loss with my own current solve: model loss 0.8764 kW vs scalar bookkeeping loss 0.4390 kW → corrected N2 **12.5514 kW = +24.10%** vs nominal 10.114 kW; warm-scalar figure 12.114 kW = +19.77%; WS1's regen-disabled cold case 15.005 kW = +48.35%. All three match the report ("12.55 kW (+24%)", "12.11 (+20%)", "+48%", headline "+20–25%").
  - **Convention ledger (F2)**: WS1 `battery_trace` confirmed stored-side at source (discharge p = net/0.97); 120.292 stored = **116.683 kW at the bus**; own re-run of the 8-seed V2 two-pass ensemble gives terminal peaks 90.73–**124.55 kW** bus = **128.40 kW** stored, exceedance **+6.74%** — matches T6's ledger, `power_convention`, T11 and ES-4 ("116.7 / 124.6 / 128.4 / +6.7%").
  - **V1 start-stop** (own hysteresis supervisor): dis 74.8–84.5 kW, chg 97.92 kW, 2.1–3.1 starts/h, cycle heat max 0.5700 kW — matches T6 and the ledger.
  - **Life**: own arithmetic from the per-seed damage in results.json with own log-interp calendar table → 8.8307 / 15.7430 yr, matching `life_projection_years`.
  - **Chemistry trade, frontier, sensitivities** (hand): LFP 204s / 13.13 kWh / 4.0 kW at −10 °C → 213.8 kWh cold-match; NMC 180s / 26.28 kWh / 13.4 kW → 127.7 kWh; hybrid 344 SC cells / 175 kg / 163 L / 42 kg DC-DC; frontier 10.05 / 14.28 / 6.62 / 29.81 kWh; LTO-10 cold discharge 134.7 kW and cold acceptance 26.9 kW; 400 V-class 110.1 kW warm / 40.5 kW cold; 2,000 m steady 52.4 °C (308 W/K); T8's 0.6331 → 1.434 V/cell footnote claim. All confirmed.
- **Compliance**: 8-seed ensemble convention on every stochastic extremum (T6/T7; the gates, descent rows and capability maps are deterministic model quantities); part-load T/SOC tables and an efficiency map, no scalars (R9); heat to the WS6 ledger by component and case, with the sizing line labelled; all five assignment tasks and all required sensitivities present (floors ×1.5, envelope corners, cell size up/down, plus the 400 V bus case); interface carries every item the assignment lists (voltage window, granularity, mass, volume, coolant, heat, regen-acceptance file); all five escalations cite rulings (ES-1 R2/R8, ES-2 R7, ES-3 R8, ES-4 R8/R9, ES-5 R5).

## Round-1 findings: resolution audit

- **F1 (blocking) — GENUINELY RESOLVED, not cosmetically.** `operating_min_V` is now 432.0 V = the same 1.50 V/cell floor every `r8_compliance` gate is computed against; recomputation confirms joint satisfiability: delivering the full 120 kW at the exported −5.5 °C gate, worst declared SOC, puts the bus at 437.50 V ≥ 432.0 V (and the C-limit check at the gate passes: 274 A vs ~317 A tapered pulse limit). The r1 gates (−5.5 / 0.0 / −10.0 °C) are unchanged, as the changelog claims. The rework goes beyond the minimum asked: T12 prices every candidate electronics floor (the r1 532.8 V value is retained as the 1.85 V/cell row, gate +9.0 °C — verified); `r8_operating_points` replaces the misleading "~210 A" rationale with warm and cold-gate currents; the **charge-side symmetric latent defect is fixed unprompted** via `transient_max_V` = 777.6 V with the verified 776.4 V gate operating point; and the 252s end of the offered series range is now an exact computed bound (verified), closing r1's interpolation warning.
- **F2 (material) — RESOLVED.** Both conventions now declared on every comparison (T6 ledger, `power_convention`, T11, §3.3, ES-4); all six numbers re-derived independently and correct; the r1 values (120.292, 124.6) are retained with conventions attached; no machine-readable value changed.
- **F3 (minor) — RESOLVED.** Cold resistance model in the loop, first-order method as suggested; 12.55 kW / +24% verified by independent recomputation; the like-for-like 12.11 kW figure is retained; headline restated +20–25%.
- **F4 (minor) — RESOLVED.** `genset_fuel_note` is now computed from the adjacent fields ("1.11 kWh … 1.39 kWh"); the stale 1.6–2.2 string is gone (verified in results.json).
- **F5 (minor) — RESOLVED.** (i) ledger sink now computes 308 W/K; (ii) AVERAGE vs SIZING LINE labels present, WS6 pointed at the sizing line; (iii) T8 footnote now says "below" the floor (verified: 1.434 V/cell).
- **F6 (minor) — RESOLVED.** `descent_sim` enforces the 50 kW rating as a hard cap with a computed friction column (code verified: `p_res = min(p_left, RESISTOR_BUS_RATING_W)`); the +20% payload sweep exists at both temperatures, uses WS1's payload and road-load conventions (verified at source), and the bound is exported as `descent_resistor_bound`. GVW rows reproduce r1's values unchanged.
- **No previously reported number silently dropped.** Every value catalogued in FINDINGS_WS3_r1's verification list reappears in r2 (cold 67.0/77.4; window set now including 432.0 and 777.6; 11.0836/280.52/185.47; 50.3/1.41/51.8; 5.39/2.24/1017 s; 250/500 s/1.11 kWh; 90.7–124.6/71.9–72.7; 97.9/84.5/2.1–3.1; 8.83/15.74; 213.8/127.7; 9.13C/210 A/6.49C/3.17C/73 A; −5.5/0.0; 10.05/14.28/6.62/29.81; 12.11/+20%; 1.367-vs-1.381 disclosure), and the changelog's r1-vs-r2 ledger is accurate.

## New findings (rework-introduced or surviving self-review)

### WS3-r2-N1 — MINOR. The literal-run `soc_ceiling_hit_s` = 384 s is the lossless ramp, not the simulated trajectory

`run_ws3.py` (literal 50 kW block) computes the exported ceiling time from a fresh zero-loss SOC ramp (`SOC_TARGET + P·k/e_np_j`), while the simulation two lines above it subtracts the I²R share; with the model's own ~1.41 kW heat the ceiling arrives at ~395 s, not 384 s. The number is quoted in T4 ("384 s unmanaged") and ES-1. No consumer inherits it and the 3% error changes nothing downstream; fix by reading the ceiling time off the simulated trajectory or labelling it "lossless bound".

### WS3-r2-N2 — MINOR. Heat-ledger line 2 is labelled "R8 peak 120 kW discharge (10 s)" but carries the ensemble-peak heat

The 14.41 kW on that line is `duty_ensemble.V2_iMMD.heat_peak_kW.max` — the instantaneous heat at the **124.6 kW** ensemble peak — not the heat at a 120 kW draw (≈13.2–14.2 kW depending on SOC). Conservative direction, ~2%, and carried over unchanged from r1; but after F2's convention discipline the label should say which quantity it is (suggest: "duty ensemble peak discharge (124.6 kW bus, 10 s class)").

Nothing else. The header's "every number … read from results.json" claim is slightly generous — the prose template in make_report.py contains hand-typed literals — but every prose literal spot-checked (≈40 of them, including all r2-changed passages, the summary, §4.1–4.2, ES-1–ES-5 and the changelog) agrees with results.json, and the 13 spliced table sections are verbatim-asserted by the build itself.

## Verdict

**No blocking or material findings.** F1 through F6 are all genuinely resolved — F1 by recomputation of the whole window/gate set from first principles, F3 and F6 by exact independent reproduction of the changed quantities (12.5514 kW / +24.10%; 44.3177 kW / 5.6823 kW margin / friction 0.00), F2 by re-deriving all six ledger numbers including the 124.55 kW ensemble peak from my own trace processing. Determinism is byte-identical, the three-way interface agreement is verbatim, no prior number was dropped, and the two new findings are minors that touch no exported interface member. The interface block as delivered is safe for WS2 to freeze the bus window against.

---

*Placement note (foreman, for the record): the adjudicator subagent's direct file write was refused by the harness; the foreman placed this document at the adjudicator's mandated path verbatim and unmodified from the adjudicator's returned text.*
