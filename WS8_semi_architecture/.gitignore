# Working file: the per-corner simulation checkpoint. It exists so an
# interrupted run resumes where it stopped; it is regenerated by
# run_ws8.py and is not part of the record.
data/_checkpoint.json
